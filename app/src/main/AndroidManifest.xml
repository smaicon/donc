<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codemaicon.donc_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activitys.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activitys.VereadoresActivity" />
        <activity android:name=".activitys.CalendarioActivity" />
        <activity android:name=".activitys.DespesasActivity" />
        <activity android:name=".activitys.NoticiasActivity" />
        <activity android:name=".activitys.ItemVereador" />
        <activity android:name=".activitys.ListaActivity" />
        <activity android:name=".activity_item_lista"></activity>
    </application>

</manifest>